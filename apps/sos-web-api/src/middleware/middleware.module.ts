import { Module } from '@nestjs/common';

@Module({
  providers: [],
  exports: [],
})
export class MiddlewareModule {
  // Simplified middleware module for now
  // The middleware functionality will be handled by NestJS built-in guards and interceptors
}
